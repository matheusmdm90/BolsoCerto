import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const settings = () => {
  return (
    <SafeAreaView>
      <Text>Ola Configurações!!</Text>
    </SafeAreaView>
  );
};

export default settings;
